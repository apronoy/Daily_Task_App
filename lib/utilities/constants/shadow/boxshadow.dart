import 'package:flutter/material.dart';

class pBoxshadow {
  static List<BoxShadow> scorCardShadow = [
    BoxShadow(
        color: Colors.black.withOpacity(0.1),
        spreadRadius: 1,
        blurRadius: 2,
        offset: Offset(2, 1))
  ];
}
